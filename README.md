This project implements Support Vector Machine (SVM) for classification.

PCA is used to reduce features to 2D.
Different parameters are tuned using GridSearchCV.

The model predicts cancer classification and outputs accuracy.
